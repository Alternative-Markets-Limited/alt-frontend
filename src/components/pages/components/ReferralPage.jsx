import React from 'react';
import { Referral } from '../../referral/components';
import { BvnRoute } from '../../common/components';

export const ReferralPage = () => <BvnRoute component={Referral} />;
